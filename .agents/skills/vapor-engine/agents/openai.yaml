interface:
  display_name: "VAPOR Engine"
  short_description: "Execute VAPOR's production DAG to the Codex audit gate"
  default_prompt: "Use $vapor-engine to execute ready graph nodes with mandatory skills, isolated specialist subagents, resumable missing inputs, evidence-gated verification, and three fresh professor audits."
